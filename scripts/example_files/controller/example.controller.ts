import { Request, Response, NextFunction } from 'express';
//import { functionName } from '@/services/module_name'; import for modul service

export const functionName = async (req: Request, res: Response, next: NextFunction) => {
  {
    /*
            try {
                const { data } = req.body;
                const { field1, field2 } = await functionName(data);
                res.json({ field1, field2 });
            } catch (err) {
                next(err);
            }
        */
  }
};
